{
  "version": 3,
  "name": "learnup-backend",
  "builds": [{ "src": "server.js", "use": "@vercel/express" }],
  "routes": [{ "src": "/(.*)", "dest": "/server.js" }],
   "rewrites": [{ "source": "/(.*)", "destination": "/" }] 
}
